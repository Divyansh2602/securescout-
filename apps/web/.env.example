# Local development: point at the API running locally (npm run dev in apps/api).
# In production this is set in the Vercel project's Environment Variables,
# NOT here — e.g. https://securescout.onrender.com
NEXT_PUBLIC_API_URL=http://localhost:4000
